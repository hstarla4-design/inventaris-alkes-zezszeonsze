services:
  api:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run dev:api"
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    volumes:
      - ./:/app
